<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Clientes</title>
</head>
<body>
    <h1>Formulário de Clientes</h1>
    <form action="./" method="post">
        <input type="hidden" name="cpf" id="cpf" value="<%= (client.length > 0 ? client[0].cpf:'') %>"><br>

        <label for="password">Senha</label>
        <input type="password" name="password" id="password" value="<%= (client.length > 0 ? client[0].senha:'') %>"><br>

        <label for="name">Nome</label>
        <input type="text" name="name" id="name" value="<%= (client.length > 0 ? client[0].nome:'') %>"><br>

        <label for="birthDate">Data de Nascimento</label>
        <input type="date" name="birthDate" id="birthDate" value="<%= (client.length > 0 ? client[0].data_nascimento:'') %>"><br>

        <label for="nationality">Nacionalidade</label>
        <input type="text" name="nationality" id="nationality" value="<%= (client.length > 0 ? client[0].nacionalidade:'') %>"><br>

        <label for="email">E-mail</label>
        <input type="text" name="email" id="email" value="<%= (client.length > 0 ? client[0].email:'') %>"><br>

        <label for="phone">Telefone</label>
        <input type="text" name="phone" id="phone" value="<%= (client.length > 0 ? client[0].telefone:'') %>"><br>

        <input type="submit" name="acao" value="Atualizar">
        <input type="submit" name="acao" value="Cancelar">
    </form>
</body>
</html>