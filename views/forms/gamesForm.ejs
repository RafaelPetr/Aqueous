<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Jogos</title>
</head>
<body>
    <h1>Formulário de Jogos</h1>
    <form action="../salvar/" method="post">

        <input type="hidden" name="id" id="id" value="<%= (game.length > 0 ? game[0].id:'') %>"><br>

        <label for="title">Título:</label>
        <input type="text" name="title" id="title" value="<%= (game.length > 0 ? game[0].titulo:'') %>"><br>

        <label for="genre">Gênero:</label>
        <input type="text" name="genre" id="genre" value="<%= (game.length > 0 ? game[0].genero:'') %>"><br>

        <label for="developer">Desenvolvedor:</label>
        <input type="text" name="developer" id="developer" value="<%= (game.length > 0 ? game[0].desenvolvedor:'') %>"><br>

        <label for="publication">Data de Publicação:</label>
        <input type="date" name="publication" id="publication" value="<%= (game.length > 0 ? game[0].data_publicacao:'') %>"><br>

        <label for="price">Preço:</label>
        <input type="float" name="price" id="price" value="<%= (game.length > 0 ? game[0].preco:'') %>"><br>

        <input type="submit" name="acao" value="Atualizar">
        <input type="submit" name="acao" value="Cancelar">
    </form>
</body>
</html>